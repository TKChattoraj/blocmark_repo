


<%= render partial: 'bookmarks/bookmarks_by_topic', locals: {topics_bookmarks_hash: @topic_bookmarks_hash} %>

<div class="row">
  <% if user_signed_in? %>
    <% if @topic.bookmarks.empty? %>
      <div class='col-md-3 col-sm-4 col-xs-4'>
        <%= link_to "Delete Topic", user_topic_path(current_user, @topic), method: :delete, class: 'btn-xs btn-danger', data: {confirm: 'Are you sure you want to delete this topic?'} %>
      </div>
    <% end %>
    <div class='col-md-3 col-sm-4 col-xs-4'>
      <%= link_to "New Bookmark", new_topic_bookmark_path(@topic), remote: true, class: 'btn-xs btn-success new-bookmark-link' %>
    </div>
    <div class='col-md-6, col-sm-4 col-xs-4'>
      <div class='js-bookmark-form'>
      </div>
    </div>
    <% end %>
  </div>

  
